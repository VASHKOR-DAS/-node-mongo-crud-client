import React from 'react';

const Header = () => {
    return (
        <div>
            <div>
                <a href="/">Home</a> {}
                <a href="/users/add">Add User</a>
            </div>
        </div>
    );
};

export default Header;